namespace ScCsgoResources;

/// <summary>Resource assembly marker only: no mod loader, blocks, gameplay or save state.</summary>
public static class ResourceMarker {
    public const int Format = 1;
}
