# 0.19.3 — 剩余 24 把枪 · 第四批：两把机枪（2026-09-05）

m249、negev。弹链机枪，没有新机制：全自动、大弹箱（100 / 150）、单段换弹（5.7 s，`WPN_SHOW_BULLETS`
把弹链露出来、`WPN_DROP_MAG` 换弹箱）。内格夫有 `shoot_empty`，空仓逻辑照旧。

## 一、数据

- 火焰：模型事件 `uweapon_muzflsh_mach` → 它唯一的火焰子系统 `uweapon_muzflsh_mach_primaryflash_alt`；
  粒子数是曲线（上一批已处理为不写该键）。
- 曳光：`weapon_tracers_mach`，rope，滚动 15500 in/s、周期 100 in、半径沿绳 1→3 in（曲线）、贴图
  `sparks` 第 4 帧（复用 `cs2_tracer_blend`）——读取器上一批已能读曲线半径，这里直接落地。
- 换弹提示音 8 条（coverup / chain / boxout / boxin / coverdown / pump），M249 的 `bullet16` 骨在换弹中位移 13.4 in
  （自检"换弹要有部件动"改成量所有部件骨后正好抓到弹链）。
- 未做：`bullet_hide_*`（打空后弹链上的子弹消失）。

## 二、驱动脚本的真正问题

三批都出现"`--cues` 装不全、要手动补一遍"，这批把安装条数打印出来后看到 `cues installed: 0`：
`cs2_sound_timings.py` 按 `guns.json` 遍历，而新枪要到最后一步 `--apply` 才进清单——第一次时序里没有
新枪的片段，安装步骤无事可做。`--apply` 现在在提示音步骤之前执行。M249 / 内格夫的提示音这次一趟装齐。

## 三、验收

对 `output/ScCsgoKnives-0.19.3.scmod`，SHA-256
`7affc91d8404235b6476ac0b8921a5d096703c04818f1e187df15d14462e1e50`，712 项 217.8 MB。

| 项目 | 结果 |
| --- | --- |
| 包内 DLL 自检 | 2211 / 2211 通过（0.19.2 为 2087） |
| csmc 回归 `verify_cs.py` | PASS，22 把刀左手误差 0.0000 |
| `videocheck.py` | 最差地标误差 24.0 px，与 0.15.10 一致 |
| CS2 自检 rig / mesh / placement / arms / effects / weapons / tracer | 全部 PASS |
| 校验器用例 | 21 / 21 |
| 提示音表 | 118 片段 466 条，466 条落到包内 OGG |

离线自检不替代实机，见第四节。

## 四、Windows 最小回归（0.19.3）

1. M249 / 内格夫：拔枪、全自动连射、曳光是短光条（M249 每发一条、内格夫每发一条）、火焰在枪口。
2. R 换弹 5.7 s：掀盖、拉弹链、换弹箱、合盖、拉栓的声音都在。
3. 内格夫打空最后一发用 `shoot_empty`。
4. 抽前几批各一把看没变。
