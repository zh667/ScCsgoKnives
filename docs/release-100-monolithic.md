# CS武器 1.0.0 单包版

主包和完整资源已合并为一个 `.scmod`。包内同时包含 `ScCsgoKnives.dll`、`ScCsgoResources.dll`、全部模型、贴图、音频、动画和资源清单；包内 `modinfo.json` 不再声明资源包依赖。

发布文件：`[API1.9]CS武器1.0.0.scmod`。安装时只放这一个文件即可，不要再同时放旧的主包或资源包。包内版本仍为1.0.0，武器存档layout5/schema4、0.28.2迁移、资源和玩法均未改变。

单包离线检查：9756项、0失败；包约330.10 MB、1127项，SHA256：`9c700bc8133942ca4fe0ce7e9604296e7acd1533f684aaa5dd4bfda252e84424`。检查覆盖了合并包内的资源程序集加载。旧的分包文件已移到项目临时备份目录，不作为发布文件。
