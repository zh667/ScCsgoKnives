# C4 落点高度修复（2026-09-13）

C4 的地面射线仍以可碰撞方块为基准；对射线命中面上方的覆盖方块，改用 `Block.GetCustomCollisionBoxes` 的实际 `Max.Y` 计算顶部高度。雪层按 0.125 格、地毯按 0.0625 格抬升，普通完整方块和高植被不会被当成覆盖层。

旧存档中已经保存的 C4 坐标在绘制时也经过同一计算，因此不需要改写世界文件。

验证：Release 编译 0 警告/0 错误；PackageCheck `failed=0`（8297 项），资源包 1200 个全量资源。
